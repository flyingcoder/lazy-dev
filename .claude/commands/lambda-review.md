---
description: Review a diff or PR using the Lambda Engine operator lens (Meta/Ortho/Para/Weave)
---

Apply the `lambda-review` skill to: $ARGUMENTS

Reflect on whether the change solves its stated problem (Meta), list
concrete file:line issues (Ortho), consider a genuine alternative approach
if one exists (Para), and integrate into one ranked review (Weave) using
this repo's CRITICAL/HIGH/MEDIUM/LOW severity levels. See
`.claude/skills/lambda-review/SKILL.md` for the full procedure and output
format.
