---
description: Run the HALIRA Protocol for a foundational contradiction or paradigm shift
---

Apply the `halira` skill to the following contradiction/paradox: $ARGUMENTS

Run the full 7-step HALIRA sequence (Potentia → Boundary → Recursion →
Integration → Anomaly → Rupture → Recognition). Step 5 (Anomaly) is
mandatory — never skip the adversarial attack on the Step 4 solution, and
never apply it directly after a Meta move (substitute Para or Retro there
instead). See `.claude/skills/halira/SKILL.md` for the full protocol and
output format.
