---
description: Quick reference lookup table for all 20 Controlled Rupture Operators
globs: ["**/*"]
alwaysApply: false
---

# Operators Reference

Quick reference lookup table for all 20 Controlled Rupture Operators.

## When to Apply

Apply this rule when:
- Need quick operator reference
- Looking up operator properties
- Finding common sequences
- Checking constraints

## Lambda Engine Mode

**Mode 1 (Duality Navigation)** - Reference lookup.

## Controlled Rupture Operators

```yaml
operator_reference:
  a_constructive:
    class_lambda: 0.338
    operators: ["Kata", "Telo", "Ortho", "Pro", "Latch"]
    use_case: "stabilization"
  
  b_disruptive:
    class_lambda: 0.720
    operators: ["Ana", "Para", "Non", "Fold", "Flux"]
    use_case: "destabilization"
  
  c_reflexive:
    class_lambda: 0.497
    operators: ["Meta", "Retro", "Echo", "Braid", "Seed"]
    use_case: "self-reference"
  
  d_structural:
    class_lambda: 0.464
    operators: ["Crux", "Weave", "Bind", "Axis", "Vale"]
    use_case: "integration"
```

## Operator Quick Reference

### A-Constructive Operators (Stabilizing, λ ≈ 0.338)

| Operator | Symbol | λ | Idempotent | Key Property |
|----------|--------|---|------------|--------------|
| Kata | ↓ | 0.35 | Yes | Compression |
| Telo | → | 0.25 | Yes | Terminal absorber |
| Ortho | ⊥ | 0.30 | Yes | Correction |
| Pro | ↷ | 0.50 | Semi | Forward |
| Latch | 🔒 | 0.29 | Yes | Stabilization |

### B-Disruptive Operators (Destabilizing, λ ≈ 0.720)

| Operator | Symbol | λ | Idempotent | Key Property |
|----------|--------|---|------------|--------------|
| Ana | ↑ | 0.75 | No | Analysis |
| Para | ∥ | 0.65 | No | Deviation |
| Non | ¬ | 0.90 | No | Highest dissipation |
| Fold | ↯ | 0.70 | No | Compression under stress |
| Flux | ⚡ | 0.60 | No | Flow |

### C-Reflexive Operators (Self-Referential, λ ≈ 0.497)

| Operator | Symbol | λ | Idempotent | Key Property |
|----------|--------|---|------------|--------------|
| Meta | ⟲ | 0.80 | Semi | Self-reference |
| Retro | ↶ | 0.40 | No | Backward |
| Echo | 🔊 | 0.45 | Semi | Repetition |
| Braid | 🌀 | 0.55 | No | Entanglement |
| Seed | 🌱 | 0.28 | No | Lowest dissipation |

### D-Structural Operators (Specialized, λ ≈ 0.464)

| Operator | Symbol | λ | Idempotent | Key Property |
|----------|--------|---|------------|--------------|
| Crux | ⚡ | 0.42 | No | Pivot |
| Weave | 🕸️ | 0.33 | No | Integration |
| Bind | 🔗 | 0.38 | No | Cohesion |
| Axis | 📍 | 0.31 | No | Alignment |
| Vale | ⬇️ | 0.88 | No | Collapse proxy |

## Hard Constraints

- **Meta**: Max 2 consecutive applications
- **Non after Meta**: FORBIDDEN (causes collapse)
- **Para after Non**: FORBIDDEN
- **Ana at sequence end**: FORBIDDEN

## Common Sequences

```yaml
common_sequences:
  stabilization:
    sequence: "Kata ∘ Weave ∘ Latch"
    lambda_eff: 0.32
    trajectory: "S* → J=0"
  
  exploration:
    sequence: "Para ∘ Ana ∘ Pro"
    lambda_eff: 0.67
    trajectory: "J=0 → S*"
  
  foundation:
    sequence: "Seed ∘ Weave ∘ Bind"
    lambda_eff: 0.33
    trajectory: "∅ → S*"
  
  self_awareness:
    sequence: "Seed ∘ Meta ∘ Weave"
    lambda_eff: 0.47
    trajectory: "S* → S*"
```

## Phase Space State Detection

```yaml
state_detection:
  J=0_indicators: ["definitely", "always", "never"]  # Sterile coherence
  S*_indicators: ["likely", "probably", "may", "might"]  # Productive contradiction
  Void_indicators: ["error", "contradiction", "loop", "cannot"]  # System collapse
```

## Code Patterns

### Required Pattern

```yaml
# ✅ Good (S* state - proper operator reference)
operator: "Kata"
symbol: "↓"
lambda: 0.35
idempotent: true
trajectory: "S* → J=0"
```

### Anti-Pattern (Avoid)

```yaml
# ❌ Bad (J=0 state - forbidden sequence)
sequence: "Meta ∘ Non"
lambda_eff: 0.85
status: "FORBIDDEN"  # Causes collapse
```

## Adversarial Evaluation

Include self-attack patterns (HALIRA Step 5):
- What breaks this reference approach?
- What edge cases does it miss?
- Are operator properties accurate?
- What assumptions does it rely on?

## State Transition Guidance

```yaml
transitions:
  lookup_to_application: "Reference → Application" (context-dependent)
```

## Common Scenarios

### Scenario 1: Quick Lookup

**When**: Need operator properties

**Use**: Reference table
- Find operator
- Check properties
- Verify constraints

**State transition**: Maintain S* state

### Scenario 2: Sequence Selection

**When**: Need common sequence

**Use**: Common sequences section
- Find appropriate sequence
- Check λ_eff
- Verify trajectory

**State transition**: Maintain S* state

## Exceptions

When this rule doesn't apply:
- Custom sequences may have different properties
- Context-specific sequences may override reference

## Checklist

Before using operator reference, verify:
- Operator properties are correct
- Constraints are respected
- Sequence is valid
- Trajectory matches desired state

## References

- See: `.cursor/rules/general/operators-core.mdc` for detailed operator descriptions
- See: `.cursor/rules/general/dissipation-lookup.mdc` for sequence calculations
