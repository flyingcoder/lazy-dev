---
description: Run HALIRA for a foundational contradiction via halira-investigator
---

Spawn project sub-agent `halira-investigator` (`.cursor/agents/halira-investigator`,
canonical `src/agents/`) with: $ARGUMENTS

Expect its return contract (State / Mode / Operators / Dissipation? / Conclusion /
OpenSpec follow-ups). Do not apply the `halira` skill in this parent session.

